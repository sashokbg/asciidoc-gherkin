import {TestCase} from "../model/test-case";
import {GherkinDocument} from "../model/gherkin-document";

describe('Test Case', () => {
    it('Show undefined status if all steps are undefined', () => {
        //const testCase = new TestCase({}, {pickle: {}}, new GherkinDocument({}));
    })
})